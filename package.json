{
  "name": "raluca-korean",
  "version": "1.0.0",
  "description": "Aplicatie de invatat coreeana pentru TOPIK",
  "private": true,
  "scripts": {
    "gen-icons": "node scripts/gen-icons.js",
    "cap-assets": "npx @capacitor/assets generate --iconBackgroundColor=#db2777 --splashBackgroundColor=#fdf4ff --splashBackgroundColorDark=#0e0020",
    "icons": "npm run gen-icons && npm run cap-assets",
    "ios": "npx cap open ios",
    "sync": "npx cap sync ios",
    "build:ios": "npm run gen-icons && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "sharp": "^0.34.5"
  }
}
