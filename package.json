{
  "name": "raluca-korean",
  "version": "1.0.0",
  "description": "Aplicatie de invatat coreeana pentru TOPIK",
  "private": true,
  "scripts": {
    "icons": "npx @capacitor/assets generate --iconBackgroundColor=#db2777 --splashBackgroundColor=#fdf4ff --splashBackgroundColorDark=#0e0020",
    "ios": "npx cap open ios",
    "sync": "npx cap sync ios",
    "build:ios": "npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/assets": "^3.0.5"
  }
}
